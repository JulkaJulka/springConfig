CREATE TABLE "FILE"
   (	ID NUMBER NOT NULL,
   CONSTRAINT FILES_PK PRIMARY KEY (ID),
	NAME VARCHAR2(10) NOT NULL,
	"FORMAT" VARCHAR2(20),
	"SIZE" NUMBER NOT NULL,
	STORAGE_ID NUMBER,
	 CONSTRAINT FILE_FK FOREIGN KEY (STORAGE_ID)
	  REFERENCES STORAGE (ID)
	  );