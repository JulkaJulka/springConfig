 CREATE TABLE FLIGHT
   (FLIGHT_ID NUMBER NOT NULL,
   CONSTRAINT FL_PK PRIMARY KEY(FLIGHT_ID),
   PLANE_ID NUMBER,
   CONSTRAINT FL_PL_FK FOREIGN KEY(PLANE_ID) REFERENCES PLANE(PLANE_ID) ON DELETE SET NULL,
   DATE_FLIGHT DATE,
   CITY_FROM NVARCHAR2(50),
   CITY_TO NVARCHAR2(50));